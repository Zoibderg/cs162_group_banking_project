#include "BalanceHandler.h"

/**
 * @brief Handles balance operations
 * @param balance The balance to process
 * @details Base implementation for balance handling operations.
 *          Derived classes can override this to implement specific handling logic.
 */
void BalanceHandler::handleBalance(double balance) {
    // Base implementation - can be overridden by derived classes
} 